import { createContext } from "react";
// estes es cm el archivo 1

// escribimos createContext para q lo importe

// definimos cual es el estado inicial q queremos q tenga
export const UserContext =  createContext()